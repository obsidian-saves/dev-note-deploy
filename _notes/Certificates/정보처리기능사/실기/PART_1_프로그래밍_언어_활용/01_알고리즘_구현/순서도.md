# 의미
> 알고리즘을 표현하는 여러 방식 중 하나. 미리 약속된 기호와 그림을 통해 ==논리적 절차==, ==흐름==, ==연산== 등을 시각적으로 표현한 것을 말한다.

![[Pasted image 20250730214759.png|500x500]]

# 순서도 연산
### 산술 연산
+ + : 더하기
+ - : 빼기
+ * : 곱하기
+ / : 나누기
+ %, mod : 나머지
+ ^ : 거듭제곱
>나머지는 순서도에서 %와 mod 둘 다 사용 가능하지만, 프로그래밍에서는 %만 사용 가능하다.

### 관계 연산
- > : 크다(초과)
- >= : 크거나 같다(이상)
- < : 작다(미만)
- <= : 작거나 같다(이하)
- = : 같다
- <>, != : 같지 않다(다르다)

# 순서도 기초 공식

### 1) 변수와 상수
- 변수 : ==고정되어 있지 않은== 값 또는 어떤 값을 ==주기억장치==에 기억하기 위해서 사용하는 ==공간==.
- 상수 : ==고정되어 있는 값== 또는 이미 ==지정되어 있는== 변하지 않는 값.
- 예를 들어 a = 10이란, 변수 a에 정수 10을 입력하라는 의미이다.
- 처리 기호 안에서 =(equal)은 같다는 의미가 아닌 대입 또는 입력으로 쓰인다.
### 2) 수의 증감
>수(i)의 초기값이 설정되어 있는 경우
- i = i + 1, i = i -1
![[Pasted image 20250730220439.png]]

>수(k)의 초기값이 설정되어 있지 않은 경우
- k = 고정값 - 변수
![[Pasted image 20250730220636.png]]

### 3) 합의 공식
- 합계 = 합계 + 수
![[Pasted image 20250730221317.png]]

### 4) 평균 공식
- 평균(avg) = 합계(sum) / 개수(cnt)
![[Pasted image 20250730221423.png]]

### 5) 부호 변경
> 부호의 변경은 판단 기호를 이용하여 계산하거나 `부호 = 부호 * (-1)`을 이용하여 연산한다.
### 6) 몫과 나머지
> 몫
- `값 / 수`로 표현한다.
- 결과값을 정수형으로 표현한다. -> int
- 4 / 2 = 2, 5 / 2 = 2
> 나머지
- `값 % 수` 또는 `값 mod 수`로 표현한다.
- 7 % 3 = 1, 5 mod 2 = 1
### 7) 배열
- 배열은 번호에 대응하는 데이터들로 이루어진 자료 구조를 말하는데, 변수를 연속적으로 저장하기 위해 확보한 메모리 공간을 의미한다.
- a[5] = a[0], a[1], a[2], a[3], a[4]의 주소로 이루어진다.
- 프로그래밍의 배열에서의 주소값은 0부터 시작이 고정이지만, 순서도에서는 0 또는 1로 시작이 가능하다.
![[Pasted image 20250730221912.png]]
